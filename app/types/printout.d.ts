export type DefinitionsOptionsType = {
  currentDate: string;
  workPlace: string;
  boraEmployeeName?: string;
  employeeSender: string;
  rcodeSender: string;
  itemsBox?: { boxNo: string; sumCard: number; }[];
  totalCard: string;
};
